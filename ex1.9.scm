; Iterative procedure
; 
;(define (+ a b)
;  (if (= a 0)
;    b
;    (inc (+ (dec a) b))))
;
; evaluate (+ 4 5) using the substituion model
; (+ 4 5)
; (:q
; 


; Recursive procecure
;
;(define (+ a b)
;  (if (= a 0)
;    b
;    (+ (dec a) (inc b))))
