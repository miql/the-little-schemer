; Iterative procedure
; 
;(define (+ a b)
;  (if (= a 0)
;    b
;    (inc (+ (dec a) b))))
;
; evaluate (+ 4 5) using the substituion model
;
;  (if (= 4 0)
;    5
;    (inc (+ (dec 4) 5))))


; Recursive procecure
;
;(define (+ a b)
;  (if (= a 0)
;    b
;    (+ (dec a) (inc b))))
